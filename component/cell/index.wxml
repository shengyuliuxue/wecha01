<view data-title="{{cell.title}}" data-status="{{cell.status}}" class="container {{outerclass}} " bindtap="choose">
  <view class="inner-container {{innerclass}}">
    <text>{{cell.title}}</text>
  </view>
</view>
